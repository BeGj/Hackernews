import { UrlPreviewPipe } from './url-preview.pipe';

describe('UrlPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
